# ezeroms legacy styles (Hugo → Next port)

This tree is the previous site CSS (`src/styles/site`), kept intact during the
Tailwind design-system migration.

- Do not delete or heavily edit files here except for critical bugfixes.
- New UI should use Tailwind + `src/components/ui` + `design-tokens.css`.
- Remove this directory once all public pages have migrated.
